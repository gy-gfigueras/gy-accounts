pipeline {
  agent any

  environment {
    
  }
  
  stages {
    stage('Test (delete this)') {
      steps {
        git branch: 'main', url: 'https://github.com/GY-CODING/gy-accounts.git'
      }
    }
  }
}
